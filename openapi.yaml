openapi: 3.0.0
info:
  title: Sample API
  version: 1.0.0
  description: This is a sample API.
paths:
  /sample:
    get:
      summary: Sample endpoint
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Hello, world!!